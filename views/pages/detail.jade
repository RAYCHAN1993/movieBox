extends ../layout

block content
	.container
		.row
			.col-md-7
				//- embed(src="#{movie.flash}", allowFullScreen="true", quality="high", width="640", height="380", align="middle",type="application/x-shockwave-flash")
				//- video(src="#{movie.flash}",autoplay="true" width="720",height="600" )
				img(src="#{movie.poster}",width="500")
			.col-md-5
				dl.dl-horizontal
				dt.text-primary 电影名称
				dd.text-muted= movie.title
				dt.text-primary 导演
				dd.text-muted= movie.director
				dt.text-primary 国家
				dd.text-muted= movie.country
				dt.text-primary 语言
				dd.text-muted= movie.language
				dt.text-primary 上映年份
				dd.text-muted= movie.year
				dt.text-primary 简介
				dd.text-muted= movie.summary

